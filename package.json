{
    "name": "address-manager",
    "version": "1.0.0",
    "engines": {
        "node": "^8.9"
    },
    "files": [
        "srv"
    ],
    "dependencies": {
        "@sap/cds": "^3.7.1",
        "express": "^4.16.4"
    },
    "devDependencies": {},
    "scripts": {
        "start": "cds run",
        "build": "cds build/all --clean",
        "deploy": "cds deploy",
        "ci-smoke": "ls"
    },
    "cds": {
        "odata": {
            "version": "v2"
        }
    }
}

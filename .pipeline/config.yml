steps:
  mtaBuild:
    buildTarget: 'CF'
    mtaBuildTool: 'classic'
  cloudFoundryDeploy:
    cloudFoundry:
      credentialsId: 'cf_deploy'
      apiEndpoint: 'https://api.cf.eu10.hana.ondemand.com'
      org: 'piper'
      space: 'infra-testing'
